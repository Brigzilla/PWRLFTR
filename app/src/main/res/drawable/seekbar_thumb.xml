<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="oval">
            <solid android:color="@color/red" /> <!-- Color of the thumb (slider) -->
            <size android:width="12dp" android:height="12dp" /> <!-- Size of the thumb (slider) -->
        </shape>
    </item>
    <item android:drawable="@android:color/transparent" /> <!-- Transparent background -->
</layer-list>
